// Problem - Removing Smallest Multiples
#include <bits/stdc++.h>
using namespace std;
int main() {
    int t;
    cin >> t;
    while(t--) {
        int n;
        vector <int> v;
        for(int i = 0; i < n; ++i) {
            int c;
            cin >> c;
            v.push_back(c);
        }
        vector <int> v2;
        for(int i = 0; i < n; ++i) {
            int c;
            cin >> c;
            v2.push_back(c);
        }
        
        string str;
        cin >> n >> str;

    }
    return 0;
}